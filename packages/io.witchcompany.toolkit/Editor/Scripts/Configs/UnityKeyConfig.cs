namespace WitchCompany.Toolkit.Editor.Configs
{
    public static class UnityKeyConfig
    {
        public const string notExistMsg = "unity key not exist";       // 존재하지 않을 경우
        public const string progressMsg = "unity key already exist";   // 심사 진행 중인 유니티 키가 있는 경우
        public const string approveMsg = "unity key review approve";   // 심사 승인된 유니티 키가 있는 경우
    }
}

